CREATE TABLE wg_wnd_hrly AS (
SELECT *
FROM crosstab(
  'select record_datetime, variable, var_value
   from wguru_flat
   where variable = 1 or variable = 2
   order by 1,2')
AS wguru_flat(record_datetime timestamp, 
			  WINDSPD numeric, 
			  WINDDIR numeric)
);